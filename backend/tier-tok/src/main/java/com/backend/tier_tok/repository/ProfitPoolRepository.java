package com.backend.tier_tok.repository;

import com.backend.tier_tok.model.entity.ProfitPoolEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProfitPoolRepository extends JpaRepository<ProfitPoolEntity, Long> {
}
